import React, { Component } from 'react'
import { StyleSheet, Text, View } from 'react-native';

export default class Metrics extends Component {
    render() {
        return (
            <View>
                <Text>Metrics Container</Text>
            </View>
        )
    }
}